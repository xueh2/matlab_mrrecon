
function [I, mapI] = View_Internal_External_LevelSet(I_internal, I_external, map, contour_intensity)
% combine the internal and external contours into one image

mapI = map;

% internal: red
% external: yellow

index = find(I_internal==contour_intensity);
I = I_external;
I(index) = 25