function [input, res, gmap] = load_results_stcnnt_inference_perf(res_dir, plot_flag)
% [input, res, gmap] = load_results_stcnnt_inference_perf(res_dir, plot_flag)

input = readNPY(fullfile(res_dir, "input"))